# orderbook-cpp
Limit order book and matching engine in C++20 with benchmarks

## Build
Requires a C++20 compiler (g++ or clang) and CMake 3.16+.

​```bash
cmake -S . -B build
cmake --build build
./build/orderbook
​```