interface verbs to be implemented:
- best(): does empty-check and returns the best (price, level)
- get_or_create(price): if exists return the price level, if not create it and return
- erase(price): erase an empty price level
- iterate in price order: self-explanatory